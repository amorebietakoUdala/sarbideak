{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.lock.index' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid" {{ stimulus_controller('toggle') }} data-action="hidden.bs.modal->toggle#resetMessage">
        <div id="content" class="container-fluid" {{ stimulus_controller('table', 
                { 'url': path('lock_index'),
                'exportName': 'locks',
            })}} 
            data-table-target="content">
            {{ include('lock/_list.html.twig', { 
                'locks': locks,
                'pageSize' : pageSize,
                'page': page,
                'sortName': sortName,
                'sortOrder': sortOrder,
            }) }}
        </div>
        {{ include('lock/_spinnerModal.html.twig') }}
	</main>
{% endblock %}