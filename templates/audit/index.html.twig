{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.lock.index' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        <div id="content" class="container-fluid" {{ stimulus_controller('table', 
                    { 'url': path('audit_index'),
                    'exportName': 'audits',
                })}} data-action="entity:success->table#refreshContent" data-table-target="content">
                {{ include('audit/_list.html.twig', { 
                    'audits': audits,
                    'pageSize' : pageSize,
                    'page': page,
                    'sortName': sortName,
                    'sortOrder': sortOrder,
                }) }}
        </div>
	</main>
{% endblock %}